Sprint_7: Автотесты для API Самоката

Набор автотестов на Python (pytest + requests) для проверки CRUD-операций курьеров и заказов.

Как запустить:
pip install -r requirements.txt
pytest tests -v --alluredir=allure-results
